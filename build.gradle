plugins {
    id 'java'
}

java.toolchain.languageVersion = JavaLanguageVersion.of(21)

base {
    archivesName = 'ViaProxyInfo'
}

repositories {
    maven { url 'https://repo.viaversion.com' }
}

dependencies {
    compileOnly 'net.raphimc:ViaProxy:[3.0.22,)'
}
